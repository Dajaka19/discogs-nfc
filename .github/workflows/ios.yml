name: Build iOS IPA (unsigned)

# Builds an UNSIGNED .ipa on a free macOS runner (public repo).
# Download it from the run's Artifacts and sign+install it with Sideloadly
# using your free Apple ID. Trigger manually from the Actions tab, or it runs
# on pushes that touch the native/build config.

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'ios/**'
      - 'capacitor.config.json'
      - 'package.json'
      - '.github/workflows/ios.yml'

jobs:
  build:
    runs-on: macos-15
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - name: Select Xcode (latest stable, fully provisioned)
        uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: latest-stable

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: npm ci

      - name: Build web assets
        run: npm run build

      - name: Stage web assets + config into iOS project
        # Plain copy instead of `cap copy`/`cap sync`: on macOS those trigger
        # extra native steps that fail in CI. The committed Xcode project +
        # CapApp-SPM already declare everything; we only need the web payload
        # and the capacitor.config.json (with server.url) inside the app.
        run: |
          rm -rf ios/App/App/public
          mkdir -p ios/App/App/public
          cp -R dist/. ios/App/App/public/
          cp capacitor.config.json ios/App/App/capacitor.config.json
          # config.xml is .gitignored (normally written by `cap copy`); the Xcode
          # project bundles it, so write the minimal Capacitor stub here.
          printf '%s\n' "<?xml version='1.0' encoding='utf-8'?>" "<widget version=\"1.0.0\" xmlns=\"http://www.w3.org/ns/widgets\" xmlns:cdv=\"http://cordova.apache.org/ns/1.0\">" "  <access origin=\"*\" />" "</widget>" > ios/App/App/config.xml

      - name: Resolve Swift packages
        run: |
          xcodebuild -project ios/App/App.xcodeproj -scheme App -resolvePackageDependencies

      - name: Build unsigned .app
        run: |
          set -o pipefail
          xcodebuild \
            -project ios/App/App.xcodeproj \
            -scheme App \
            -configuration Release \
            -destination 'generic/platform=iOS' \
            -derivedDataPath build \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGN_IDENTITY="" \
            build 2>&1 | tee xcodebuild.log

      - name: Upload build log on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: xcodebuild-log
          path: xcodebuild.log
          if-no-files-found: ignore

      - name: Publish build log to ci-log branch on failure
        if: failure()
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          mkdir -p /tmp/lp && cp xcodebuild.log /tmp/lp/ 2>/dev/null || echo "no log" > /tmp/lp/xcodebuild.log
          cd /tmp/lp
          git init -q
          git checkout -q -b ci-log
          git add xcodebuild.log
          git -c user.email=ci@ci -c user.name=ci commit -qm "build log ${GITHUB_SHA}"
          git push -f "https://x-access-token:${GH_TOKEN}@github.com/${GITHUB_REPOSITORY}.git" ci-log

      - name: Package unsigned IPA
        run: |
          cd build/Build/Products/Release-iphoneos
          mkdir Payload
          cp -R App.app Payload/
          zip -qr Vinyl-unsigned.ipa Payload
          ls -la

      - uses: actions/upload-artifact@v4
        with:
          name: Vinyl-ipa
          path: build/Build/Products/Release-iphoneos/Vinyl-unsigned.ipa
          if-no-files-found: error
